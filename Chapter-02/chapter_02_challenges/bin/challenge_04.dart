void challenge4() {

  // initializes a constant variable and assigns double value to it
  const rating1 = 13.69;

  // initializes a constant variable and assigns double value to it
  const rating2 = 5.41;

  // initializes a constant variable and assigns double value to it
  const rating3 = 9.78;

  // finds the average of 3 constant double variables which by logic is a constant
  // and assigns the value to a constant double variable
  const average = (rating1 + rating2 + rating3) / 3;

  // prints constant double variable average
  print("Challenge-4: average = $average");
}

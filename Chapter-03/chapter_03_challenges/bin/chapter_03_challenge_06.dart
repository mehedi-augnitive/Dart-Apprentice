void challenge6() {
  // initializes a constant integer variable
  const number = 10;

  // initializes a constant integer variable
  const multiplier = 5;

  // initializes a final(constant) String variable during runtime
  final summary = '$number \u00D7 $multiplier = ${number * multiplier}';

  // prints the value of summary
  print("\tChallenge-6, Value of summary is: $summary");
}
